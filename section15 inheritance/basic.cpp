#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main(){
//inheritance is method of creating new classes from existing classes
//new classes contains attributes and methods of class it has inherited from
//class being extended or inherited from is called parent or super(base) class
//class being created is child or sub (derived) class
// *the relationship is "Is-A" eg A circle is a shape(circle class subclass of shape)

// *Public Inheritance vs Composition
// both allow reuse of existing class
// *Public inheritance:"is-a" relationship(employee is a person)
// *Composition:"has-a" relationship(person has a account)
    return 0;
}